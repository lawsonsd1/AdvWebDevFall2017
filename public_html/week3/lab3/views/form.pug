extends layout

block content
    h2= title
    form(action="/form", method="post")
        label Select Number 
        input(type="text", name="number")
        input(type="submit")

    if errors 
      ul
        for error in errors
          li!= error.msg 